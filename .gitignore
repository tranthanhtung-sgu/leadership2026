# API keys — never commit
.streamlit/secrets.toml

# Local copy of boss's original zip (do not publish)
leadership-streamlit-main-original/

# Local Streamlit chat debug log (LEADERSHIP_CHAT_DEBUG=0 to disable; default on)
chat_debug.log
chat_debug.log.*

# Python / editor noise
__pycache__/
*.py[cod]
.vscode/
